apiVersion: 1

providers:
  - name: alfred-writer
    folder: Alfred Writer
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
